# Editor
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local dev artefacts
/tmp/
/dist/

# Superseded Kura Cloudflare Workers scaffold — archived on branch archive/kura-cloudflare-website
/website/
/build/

# SwiftPM build output (gui/ClikaeMenuBar)
.build/
**/.swiftpm/

# Test outputs
/tests/.bats-tmp/

# Backups created by clikae itself (shouldn't be in repo, but just in case someone runs it inside the checkout)
*.clikae.bak.*

# Internal conversation backups — never publish
conversations/
DOGFOOD-*.md
