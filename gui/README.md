# gui

Reserved for v1.0 — SwiftUI macOS menu bar app.

Idea: a tiny menu bar icon showing the currently active profile (per CLI),
with quick switching, profile creation, and launching the matching `.app`.

This is a stretch goal — the CLI is the source of truth; the GUI just calls it.

PRs from Swift developers welcome.
